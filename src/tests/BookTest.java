package tests;

import model.Book;

import java.util.ArrayList;
import java.util.List;
 /*
public class BookTest {
        public void testGetAvailableBooksByAuthor() {
        return null
}
            // Создаем объекты класса Book (вместо book)

            Book book1 = new Book("автор1", "книга1", true);
            Book book2 = new Book("автор2", "книга2", false);
            Book book3 = new Book("автор1", "книга3", true);

            // Создаем список книг
            List<Book> books = new ArrayList<>();
            books.add(book1);
            books.add(book2);
            books.add(book3);
            */
            // Получаем список незанятых книг по автору "автор1"
          //  List<Book> availableBooks = getAvailableBooksByAuthor(books, "автор1");

            // Проверяем, что список содержит ожидаемое количество книг
          //  assertEquals(2, availableBooks.size(), "Должно быть 2 незанятых книги по автору \"автор1\"");

